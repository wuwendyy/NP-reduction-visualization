# NP-reduction-visualization

### Setup
Conda Env (install conda if not already)
```
conda create -n npvis python=3.12
conda activate npvis
```

Install pip Dependency
```
cd NP-reduction-visualization
pip install -r requirement.txt
```
